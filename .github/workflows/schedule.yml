name: Schedule Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule test
        run: |
          echo "==================================="
          echo "SCHEDULE WORKED"
          echo "Time: $(date -u)"
          echo "==================================="
